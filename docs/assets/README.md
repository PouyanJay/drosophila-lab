# Repository imagery

`workspace.png` is a screenshot of the real local workspace at 1440 × 920. It shows the initial composer and MaleCNS anatomy, with no experiment metrics displayed. Capture uses an isolated browser context with empty history responses to avoid including personal conversations; it does not replace anatomy or generate scientific measurements. The development toolbar is hidden for the capture.

The README uses the existing neural-fly logo from `public/brand/neural-fly.svg`. Data attribution remains in the root README and source manifests.
