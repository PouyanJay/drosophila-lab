# Candidate inspection

Make candidate rows keyboard/click selectable; distinguish inspected candidate from the validation finalist. Update chart identity and never show another candidate's curve. Inspect source anchors over transparent anatomy, with legible markers, cumulative edit counts, missing-data states and an exit action. Preserve scientific provenance and never invent engineered anatomy or rewire paths.

Extension: recover all saved per-candidate training curves by phase/seed; make source markers clickable with actual source body IDs, copy counts and recorded operations. Use themed accessible popovers rather than native dropdowns. Preserve current live measurements and ownership/path controls.
