# Atlas visual quality

User authorized implementation: rebuild visual quality toward EveryNeuron, in dark mode, inside the existing workspace. Discovery-service planning remains a separate unimplemented workstream.

## Acceptance

- Real source morphology rendered as shaded rounded branches, with richer representative coverage and clear sampling attribution.
- Dark anatomical viewport, readable depth, crisp selection and navigation. Preserve existing chat, inventory, clipping, comparison and fallback behavior.
- Generate a separate versioned display dataset from public MaleCNS SWCs with source radius estimates, hashes, units and simplification metadata. Do not change trainer data, archived engines or existing skeleton buffers.
- Progressive bounded loading; explicit partial/error state; GPU resources disposed on unmount. No scientific activity or invented anatomy implied by rendering.
- Verify actual browser output and controls, meaningful geometry/provenance tests, lint/types/build, independent reviews. No commit/push requested.

The first implementation establishes a representative high-quality atlas, not full-resolution rendering of every neuron or all features of the reference website. Implement original rendering code; do not copy reference-site code/assets.
