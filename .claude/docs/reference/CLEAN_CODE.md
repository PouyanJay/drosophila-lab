# Clean Code

Keep cohesive modules, explicit dependencies, and useful names. Extract repeated behavior when it has a stable shared responsibility; do not impose one-public-symbol-per-file or interfaces without a substitution need. Review error propagation, cleanup, concurrency, and import direction. Preserve CSS evaluation order and observable behavior during refactors. Existing debt is context, not permission to introduce more.
