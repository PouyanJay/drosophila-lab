# Compact discovery brief

Remove redundant task selection and verbose always-visible copy. Keep three composer suggestions as the task choices. Replace saved dropdown with inline bounded history. Preserve advanced configuration, method caveats, errors and running controls.
