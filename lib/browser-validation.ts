import {discoverySchema} from './discovery-contract';
import {labConfigSchema} from './lab-contract';
const finite=(v:any,lo:number,hi:number)=>typeof v==='number'&&Number.isFinite(v)&&v>=lo&&v<=hi;
export function validSession(d:any){return d&&(d.discoveryMode===undefined||typeof d.discoveryMode==='boolean')&&(d.discoveryReady===undefined||typeof d.discoveryReady==='boolean')&&(!d.discoveryConfig||discoverySchema.safeParse(d.discoveryConfig).success)&&(!d.discoveryId||typeof d.discoveryId==='string'&&/^[a-f0-9-]{36}$/.test(d.discoveryId))&&(!d.computeId||typeof d.computeId==='string'&&/^[a-f0-9-]{36}$/.test(d.computeId))&&(!d.labConfig||labConfigSchema.safeParse(d.labConfig).success)&&(!d.execution||['lab','browser'].includes(d.execution))&&(!d.selectedJob||typeof d.selectedJob==='string'&&/^(example|[a-f0-9-]{36})$/.test(d.selectedJob))&&Number.isInteger(d.stage)&&d.stage>=0&&d.stage<=4&&Array.isArray(d.messages)&&d.messages.length<=10000&&d.messages.every((m:any)=>['user','assistant'].includes(m.role)&&typeof m.text==='string'&&m.text.length<=16000)&&d.plan&&['','beacon','evidence','rule'].includes(d.plan.task)&&finite(d.plan.duplicates,0,512)&&[0,.15,.3].includes(d.plan.prune)&&[1,3,5].includes(d.plan.seeds)&&finite(d.plan.epochs,5,100)&&['','quick','study'].includes(d.plan.budget);}
export function validBrowserResult(d:any){
 if(!d||d.schema!=='malecns-browser/1.0'||d.status!=='completed'||d.graphSha256!=='729b2b60c7759ead12163cc30daa2b8a3abf8565f0f5773f19fde20cfaa14f7b'||!d.config||!['beacon','evidence','rule'].includes(d.config.task)||![1,3,5].includes(d.config.seeds)||!Number.isInteger(d.config.epochs)||!finite(d.config.epochs,5,100)||!finite(d.delta,-1,1)||!finite(d.speedup,.000001,1e6)||!Array.isArray(d.models)||d.models.length!==2||!d.protocol||!Array.isArray(d.protocol.sizes)||d.protocol.sizes.length!==3)return false;
 return d.models.every((m:any)=>typeof m.name==='string'&&m.name.length<=80&&finite(m.neurons,165122,165634)&&finite(m.edges,0,2e7)&&finite(m.accuracy,0,1)&&finite(m.latencyMs,.000001,1e8)&&finite(m.ablationAccuracy,0,1)&&Array.isArray(m.runs)&&m.runs.length===d.config.seeds&&m.runs.every((r:any)=>finite(r.testAccuracy,0,1)&&finite(r.validationLoss,0,1e4)&&Array.isArray(r.weights)&&r.weights.length===55&&r.weights.every((v:any)=>finite(v,-1e6,1e6))&&r.normalization&&['mu','sd'].every(k=>Array.isArray(r.normalization[k])&&r.normalization[k].length===55&&r.normalization[k].every((v:any)=>finite(v,-1e6,1e6)))&&Array.isArray(r.curve)&&r.curve.length===d.config.epochs+1&&r.curve.every((p:any)=>['trainLoss','validationLoss','trainAccuracy','validationAccuracy','epoch'].every(k=>finite(p[k],0,1e4)))&&Array.isArray(r.trials)&&r.trials.length===d.protocol.sizes[2]&&r.trials.length<=64&&r.trials.every((t:any)=>Array.isArray(t.xs)&&t.xs.length===6&&t.xs.every((v:any)=>finite(v,-10,10))&&[0,1].includes(t.y)&&finite(t.probability,0,1)&&typeof t.correct==='boolean')));
}
