# Python

Use the uv-managed Python 3.11 environment and committed lock. Type new boundary code and validate external payloads; follow existing domain representations rather than introducing an ORM. Avoid blocking the FastAPI event loop with long training; preserve the separate durable worker. Release files, connections, locks, and subprocesses on failure. Numerical kernels need gradient/direction/seed tests. Hashed worker sources require provenance review before edits or formatting.
