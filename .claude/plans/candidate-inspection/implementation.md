# Progress

Implemented row and arrow selection with candidate-specific curve availability; transparent inspection context and source rings replace square overlays. Show located source count, cumulative copy/rewire totals, unavailable topology, and exit control. Selecting a saved campaign clears prior inspection. Inspection stops auto-rotation and preserves user layer settings on exit.

Independent review identified stale campaign inspection, stale rotation UI, and unstable camera-fit dependencies; corrected. Historical per-candidate curves are absent from the current API; UI explicitly reports unavailability rather than showing another candidate's measurements. Rewire graph indices are not source body IDs and are not drawn as invented paths.

Validation complete: expanded browser fixture passed after review fixes, covering row click, arrow, keyboard, chart mismatch, source mapping and exit along with existing theme/mobile/state checks. Inspected the rendered transparent-anatomy screenshot. make lint (6/6), make test-web and make build passed; git diff --check passed. No paid inference, training, commit or push performed.

Follow-up visual fix: candidate and anatomical cards share a responsive stack. Compact spacing keeps the complete anatomical card visible with evidence open; overflow scrolls within each card so its lower border is never clipped by the stack. Detailed source explanation is expandable. Browser regression selects GNG, checks equal card widths/non-overlap and complete frame containment with disclosure open/closed, and checks mobile layout. Passed; desktop screenshot visually confirms the Discuss action and bottom border are visible. Lint passed.

Historical curves and interactive source inspection: added owner-scoped read-only candidate curves endpoint over saved metrics/training snapshots, with bounded phases/seeds/files and traversal controls. No numerical source changes. UI selects saved training phase/seed, merges live progress, reports loading/errors with retry, and recovers pilot topology. Numbered projected source buttons and matching themed Radix picker expose source IDs, cumulative copy counts and actual graph node/operator records. Markers are clipped to the candidate viewport in Compare. Engineered positions and per-source performance are not invented.

Independent review found live snapshots freezing, Compare viewport leakage and discarded pilot topology; fixed all three. Twelve discovery Python tests passed, plus web tests, lint, build and extended browser fixture (historical curves, seed switching, pilot topology, marker clicks, source menu, dark/light/mobile). Restarted local stack with make start; read-only real campaign verification returned 4 saved runs / 52 points for each of candidate-0 through candidate-4. No new user campaign launched and no paid inference. Changes uncommitted.

Visual change explanation: selecting a source now anchors a Before/After wiring diagram to its atlas marker. Original wiring stays neutral; additions appear in amber with a copy count and recorded self-loop count. The diagram explicitly labels its connections/placement as illustrative, supports reduced motion, and can be dismissed. It represents cumulative additions relative to the source brain, not per-copy causal performance. On compact atlas panels it temporarily replaces the overlapping inspector until dismissed. Independent review caught mixed-operator loop attribution and short-viewport clipping; loop counts and bounded scrolling correct those cases. Browser verification covers toggles, marker selection, dismissal and mobile bounds; browser fixture, lint (6/6), production build and diff checks passed. Desktop and mobile screenshots visually inspected. Changes remain uncommitted.

Placement refinement: moved Before/After diagram into the left candidate/source card, replacing the simple source-to-copy summary. Recorded edit rows now expand on demand. Removed floating callout, leader, camera-driven placement and compact-screen inspector hiding. Diagram remains accessible with the existing bounded card scrolling. Browser desktop/mobile checks and lint passed; screenshot confirms no overlay over the atlas toggle.
